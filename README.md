# plasma

# How to run
## Backend
1. Create `.env.docker` file based on example.
1. Build server image
    ```bash
    docker compose build server
    ```
1. Run backend
    ```bash
    docker compose run
    ```
